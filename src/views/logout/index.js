import { Typography } from "@mui/material";

import MainCard from "ui-component/cards/MainCard";

const Logout=()=>(
    <MainCard title='Logout'>
        <Typography variant="body-2">
            hyyy
        </Typography>
    </MainCard>
)
export default Logout